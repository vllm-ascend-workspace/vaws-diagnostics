[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vaws-diagnostics"
version = "0.2.1"
description = "Bounded structured diagnostics and safe support bundles for VAWS owners"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[project.scripts]
vaws-diagnostics = "vaws_diagnostics.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["vaws_diagnostics"]
